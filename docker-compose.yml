version: "3.8"
services:
  app:
    image: golang:1.21-alpine
    command: go run .
    tty: true # コンテナの永続化
    working_dir: /go/src/github.com/kappa-lab/gomponents-playground/
    volumes: 
      - ./:/go/src/github.com/kappa-lab/gomponents-playground/
    ports:
      - 8080:8080